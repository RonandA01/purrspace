// Session state is managed by SessionProvider (React Context).
// Re-export useSession here so all existing imports continue to work
// without change.
export { useSession } from "@/components/SessionProvider";
