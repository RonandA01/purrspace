import { ComingSoon } from "@/components/ComingSoon";

export default function ProfilePage() {
  return (
    <ComingSoon
      title="Profile"
      emoji="🐱"
      description="Your public PurrSpace profile — bio, posts, and all your cat credentials."
    />
  );
}
